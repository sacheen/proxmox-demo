services:
  monitor:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - monitor-data:/app/data
    environment:
      - DATABASE_URL=/app/data/monitor.db
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  monitor-data:
