services:
  monitor:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=/app/data/monitor.db
      - NODE_ENV=production
    restart: unless-stopped
